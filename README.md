## pure
`pure` is a minimal utility that removes ANSI escape color codes from text input,
outputting clean, unformatted text. It's perfect for processing colored terminal
output for logging or further text manipulation.
